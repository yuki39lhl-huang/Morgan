## 2026-04-08 — 整点播报与模型切换

- Fixed duplicate process issue in `kj` script by improving `kill_process` to avoid self-matching
- Resolved hourly report issues: confirmed 15:00 and 19:14 reports sent successfully; SSL proxy instability and expired DashScope API key were root causes of incomplete data
- Replaced expired DashScope API key (`sk-e2e9a077...`) with new key (`sk-745edf23...`) and updated model from `qwen3.6-plus` to `qwen-plus` in signal monitor config
- Updated OpenClaw's default LLM from `xiaomi/mimo-v2-pro` to `qwen/qwen-plus` in config
- Signal monitor restarted with new config; Gateway restart required for default model change to take effect
- Confirmed all core services (signal monitor, auto trade, news fetcher, watchdog, proxy) are running cleanly with no duplicates