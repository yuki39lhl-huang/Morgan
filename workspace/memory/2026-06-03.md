# 2026-06-03

## 代码修改（crypto_signal_monitor.py，共5处）

### 1. `calc_tp_sl()` — ATR 动态止损（~572行）
- 原：固定 -2% 止损 `entry * 0.98`
- 改：`max(2%, ATR% × 分层倍数)`，正常行情仍是 -2%，高波动自动放宽
- 使用已有配置 `atr_sl_tiers`: <2%ATR→1.0x, <4%→1.5x, ≥4%→2.0x

### 2. SL 触发后 API 验证恢复（~2295行）
- 原：平仓不管成败直接删本地持仓 → 平仓失败导致死循环
- 改：平仓后查 Binance API 确认仓位真没了才删；失败则恢复本地缓存，日志标记 `⛔`

### 3. `sync_stop_loss_to_binance` + `submit_initial_algo_orders` — 精准替换（~708 & ~740行）
- 原：`cancel_algo_orders`（全删所有Algo单）→ 重设止损+止盈 → 一个失败导致另一边也被删 → "总是漏掉止盈止损"
- 改：新增 `_cancel_algo_by_type()` 辅助函数，止损止盈分开取消和提交，互不影响

### 4. `close_position` — reduceOnly 降级链（~1197行）
三层降级：
- reduceOnly → -2022（币安测试网bug）→ 降级不带 reduceOnly
- -4164（保证金/名义价值不足）→ 补 5 USDT 到逐仓钱包
- -4164 + 名义 < $20 → 买入 $20 名义价值撑大 → 全平

### 5. API 同步重建持仓用 `calc_tp_sl`（~2265行）
- 原：硬编码 -2% / +4%
- 改：调用统一 `calc_tp_sl()` 函数

## 发现的问题

### 币安测试网 reduceOnly 全线挂了
- 所有币种 reduceOnly 订单返回 -2022 "ReduceOnly Order is rejected"
- 不分 MARKET/LIMIT，不分 LONG/SHORT → 测试网 API 问题，非代码 bug

### ETH 小仓位平不掉（已手动解决）
- ETH 持仓 0.008（名义 $14.79 < $20），reduceOnly 被拒，不带 reduceOnly 报 -4164
- 解决：买入 0.011 ETH（$20+ 名义）→ 卖出总仓位 → 清空

## 当前状态
- 全部持仓已清空（0 持仓，今日已实现 -110.42 USDT）
- monitor 老代码运行中，新代码需 `kj` 重启后生效
- reminder_scheduler.py 存活正常
- 提示老公吃了午饭（11:21）
