# 2026-06-05 日志

## 条件委托系统大修（7 个 bug）

告警：4仓 9单条件委托 → 3仓废单 qty=0 + BNB孤儿单 + DOT方向错误。

### 修复清单

#### binance_auto_trade.py (5项)
1. **`cancel_algo_orders` 假删修复** — 旧 `DELETE /algoOpenOrders` 不生效，改为 GET列表→逐笔 DELETE
2. **`cancel_all_orders` 同时清条件单** — 原来只删 /allOpenOrders(普通单)，条件单残留
3. **`place_order` 传 quantity** — 开仓时调 algo 单没传 quantity，走 closePosition=true → 测试网 qty=0
4. **`place_algo_conditional_order` 触发价精度** — 加 `get_price_precision()` 按币种格式化，修复 DOT -1111
5. **cancel -2011 降级** — 已不存在的单不算 error，改 log.info

#### crypto_signal_monitor.py (2项)
6. **`submit_initial_algo_orders` 全清后重建** — 方向反转时先 cancel_algo_orders() 全部删清，再建新单
7. **平仓后清理条件单** — TP1/SL 触发平仓后调 cancel_algo_orders()，防止 BNB 式孤儿单

### 当前状态
- Binance 测试网 Algo POST 接口全线 -1007 超时，GET/DELETE 正常
- 代码修复已验证生效（DOT 精度从 -1111 → -1007，取消逻辑干净）
- 等 Binance 恢复后 monitor 自动重建条件单

### 当前持仓
BTC SHORT / DOT SHORT / BNB SHORT（3仓，LINK 已平）

### Binance 测试网故障
- 15:01 仍在持续：Algo Order POST 接口自 14:20 起全线 -1007 超时
- GET/DELETE 正常，仅 POST 新建条件单失败
- monitor 循环重试中，恢复后自动补单

### 新增函数
- `get_price_precision(symbol)` — 按币种返回价格 tickSize 精度（BTC:1, DOT:3, LINK:3 等）
- 取代全局 `format_price()` 在 algo 条件单触发价格式化中的使用
